public class TernaryOperator
{
	public static void main(String... args)
	{
		String s = true ? "Hello" : "World"; // It must return some value(it can't used for function calling/ performing operation etc)
		System.out.println(s);
	}
}
